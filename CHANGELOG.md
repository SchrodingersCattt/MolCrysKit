# Changelog

## Unreleased

### Removed

- `create_defect_structure()` was removed because it was a non-functional
  placeholder that returned its input unchanged. Use `generate_vacancy()` for
  spatial molecular vacancies or `carve_void()` for shaped stoichiometric
  voids.
